{
  "name": "databridge-bt-dial",
  "version": "1.0.0",
  "description": "Localhost BT dial server for DataBridge (Windows): dials a number on a paired button/feature phone over Bluetooth RFCOMM (ATD).",
  "main": "server.js",
  "license": "UNLICENSED",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "optionalDependencies": {
    "bluetooth-serial-port": "^2.2.7"
  },
  "notes": "bluetooth-serial-port is Windows-only (node backend). macOS uses bt-dial/mac/bt-dial-cli instead — no npm install needed on Mac."
}
